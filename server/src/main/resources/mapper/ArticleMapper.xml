<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.server.mapper.ArticleMapper">

    <select id="findById" resultType="com.example.server.entity.Article">
        SELECT * FROM article WHERE id = #{id}
    </select>

    <select id="findPage" resultType="com.example.server.entity.Article">
        SELECT * FROM article
        <where>
            <if test="keyword != null and keyword != ''">AND title LIKE CONCAT('%',#{keyword},'%')</if>
            <if test="category != null and category != ''">AND category = #{category}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
        ORDER BY publish_time DESC LIMIT #{offset}, #{limit}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM article
        <where>
            <if test="keyword != null and keyword != ''">AND title LIKE CONCAT('%',#{keyword},'%')</if>
            <if test="category != null and category != ''">AND category = #{category}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO article(title, cover_image, category, content, view_count, status, publish_time)
        VALUES(#{title}, #{coverImage}, #{category}, #{content}, #{viewCount}, #{status}, #{publishTime})
    </insert>

    <update id="update">
        UPDATE article
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="coverImage != null">cover_image = #{coverImage},</if>
            <if test="category != null">category = #{category},</if>
            <if test="content != null">content = #{content},</if>
            <if test="status != null">status = #{status},</if>
            <if test="publishTime != null">publish_time = #{publishTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM article WHERE id = #{id}
    </delete>

    <update id="incrementViewCount">
        UPDATE article SET view_count = view_count + 1 WHERE id = #{id}
    </update>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM article
    </select>

</mapper>
